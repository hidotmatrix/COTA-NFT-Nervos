/// <reference types="@nervosnetwork/ckb-types" />
import { Byte, Service } from '../..';
import { CotaInfo } from '../../types';
export declare const generateDefineCotaTx: (service: Service, cotaLock: CKBComponents.Script, total: number, confiure: Byte, cotaInfo: CotaInfo, fee?: bigint, isMainnet?: boolean) => Promise<{
    rawTx: {
        version: string;
        cellDeps: CKBComponents.CellDep[];
        headerDeps: any[];
        inputs: {
            previousOutput: CKBComponents.OutPoint;
            since: string;
        }[];
        outputs: CKBComponents.CellOutput[];
        outputsData: string[];
        witnesses: any[];
    };
    cotaId: string;
}>;
