/// <reference types="@nervosnetwork/ckb-types" />
import { Service } from '../..';
import { IssuerInfo } from '../../types';
export declare const generateIssuerInfoTx: (service: Service, cotaLock: CKBComponents.Script, issuerInfo: IssuerInfo, fee?: bigint, isMainnet?: boolean) => Promise<{
    version: string;
    cellDeps: CKBComponents.CellDep[];
    headerDeps: any[];
    inputs: {
        previousOutput: CKBComponents.OutPoint;
        since: string;
    }[];
    outputs: CKBComponents.CellOutput[];
    outputsData: string[];
    witnesses: any[];
}>;
