/// <reference types="@nervosnetwork/ckb-types" />
import { Service } from '../..';
import { MintCotaInfo } from '../../types/service';
export declare const generateMintCotaTx: (service: Service, cotaLock: CKBComponents.Script, mintCotaInfo: MintCotaInfo, fee?: bigint, isMainnet?: boolean) => Promise<{
    version: string;
    cellDeps: CKBComponents.CellDep[];
    headerDeps: any[];
    inputs: {
        previousOutput: CKBComponents.OutPoint;
        since: string;
    }[];
    outputs: CKBComponents.CellOutput[];
    outputsData: string[];
    witnesses: any[];
}>;
